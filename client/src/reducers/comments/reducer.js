const initialState = {
  comments: [
    {
      id: 1,
      postId: 1,
      text: '1st comment',
    },
  ],
}

export default (state = initialState, action) => {
    switch (action.type) {
      default:
        return state
    }
  }
